"use client";
import { CinematicIntro } from "@/components/ui/CinematicIntro";

/**
 * Mounts the cinematic intro overlay.
 * Add to a page where you want the intro to appear.
 */
export function CinematicWrapper() {
  return <CinematicIntro />;
}
